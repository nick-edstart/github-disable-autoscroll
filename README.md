
# GitHub Actions Log UX Fix — Chrome Extension

This Chrome extension improves the GitHub Actions run UI by automatically collapsing failed `<details>` sections *only when appropriate*, reducing annoying automatic scrolling and large DOM reflows.

## 📁 Install

1. Clone or download the repo
2. Open `chrome://extensions`
3. Enable **Developer mode**
4. Click “Load unpacked” and select this directory
5. Navigate to a GitHub Actions job page to test

---

[1]: https://github.com/orgs/community/discussions/156188 "Unexpected and annoying scrolling behaviour when viewing github action logs · community · Discussion #156188 · GitHub"
